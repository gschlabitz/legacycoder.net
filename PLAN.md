# Plans
- standardize blog post filenames/slugs to English (drop the migrated German
  text, e.g. `2008-03-21-flut-2008-flood-2008` → `2008-03-21-flood-2008`);
  rename EN and DE counterpart files identically so locale fallback keeps
  matching. Do this while the site is still unpublished — filenames are
  forever once URLs are public.
- photos fly in from left and right sides?
- if hovering/clicking in a work item (left), the details show on the right (covering up life)
- vice versa
